import { FC } from "react";

import { HomePageContainer } from "./styles";

export const HomePage: FC = () => {
  return (
    <HomePageContainer>
      <section>
        <h1>catalogApp</h1>
        <small>One App to handle and manage stock easily</small>
      </section>
    </HomePageContainer>
  );
};
